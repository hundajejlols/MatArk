# File Change Tracker

This is a simple terminal application that checks if a file's content has changed since the last time it was read, using SHA-256 hashing.

## Solution Location
The entire solution for this project is located in the `main.py` file.


## How to Run
Run the program in your terminal by passing the target file path as an argument:

`python main.py <filename>`
